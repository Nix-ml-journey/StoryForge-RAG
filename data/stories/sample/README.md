# Sample stories (safe for git)

This folder holds **anonymous demo text** so reviewers can see the ingest format without your private corpus.

Your full story files live in `data/stories/*.txt` (gitignored). Copy these samples or add your own `.txt` files locally for RAG runs.
