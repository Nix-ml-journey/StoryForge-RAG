# Sample ingest manifest

One-line-per-chunk JSONL matching `records_to_ingest_manifest`. Rebuild yours with `py scripts/records_to_ingest_manifest.py` after the JSON looks right.

Full prep steps: [`docs/DATA_PREP.md`](../../../docs/DATA_PREP.md). Your full manifest is `data/ingest/ingest_manifest.jsonl` (gitignored).
