# Sample story_json (safe for git)

This folder holds a **minimal example record** matching the shape produced by `scripts/prepare_story_records.py`.

Your enriched records live in `data/story_json/*.json` (gitignored). Rebuild them locally from `data/stories/` using the Step 1 scripts.
